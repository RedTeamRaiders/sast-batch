FROM python:3.12-slim

RUN apt-get update && apt-get install -y --no-install-recommends git curl unzip \
    && curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o /tmp/awscliv2.zip \
    && unzip -q /tmp/awscliv2.zip -d /tmp && /tmp/aws/install \
    && rm -rf /tmp/awscliv2.zip /tmp/aws \
    && pip install --no-cache-dir parliament \
    && rm -rf /var/lib/apt/lists/*

COPY iam_check_entrypoint.sh /app/entrypoint.sh
COPY summarize_findings.py /app/summarize_findings.py
COPY scan_iam_policy.py /app/scan_iam_policy.py
RUN sed -i 's/\r$//' /app/entrypoint.sh && chmod +x /app/entrypoint.sh

ENTRYPOINT ["/app/entrypoint.sh"]
